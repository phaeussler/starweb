<%= form_with(model: film, local: true) do |form| %>
  <% if film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(film.errors.count, "error") %> prohibited this film from being saved:</h2>

      <ul>
      <% film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :film_name %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.date_select :year, id: :film_year %>
  </div>

  <div class="field">
    <%= form.label :director %>
    <%= form.text_field :director, id: :film_director %>
  </div>

  <div class="field">
    <%= form.label :productor %>
    <%= form.text_field :productor, id: :film_productor %>
  </div>

  <div class="field">
    <%= form.label :episode %>
    <%= form.text_field :episode, id: :film_episode %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
